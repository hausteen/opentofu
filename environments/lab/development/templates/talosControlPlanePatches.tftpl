machine:
  install:
    image: ${image}
  network:
    interfaces:
      - deviceSelector:
          physical: true
        vip:
          ip: ${vip}
  time:
    servers:
      - time.cloudflare.com
      - time.nist.gov
